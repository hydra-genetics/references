$schema: "https://json-schema.org/draft/2020-12/schema"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    description: default resource definitions
    properties:
      mem_mb:
        type: integer
        description: memory in MB used per cpu
      mem_per_cpu:
        type: integer
        description: memory used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time
    required:
      - mem_mb
      - mem_per_cpu
      - partition
      - threads
      - time

  deepsomatic_pon:
    type: object
    description: resource definitions for deepsomatic_pon
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  ichorcna_offtarget_read_counter:
    type: object
    description: resource definitions for ichorcna_offtarget_read_counter
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  ichorcna_offtarget_wig_list:
    type: object
    description: resource definitions for ichorcna_offtarget_wig_list
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  ichorcna_offtarget_panel_of_normals:
    type: object
    description: resource definitions for ichorcna_offtarget_panel_of_normals
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

  jumble_count:
    type: object
    description: resource definitions for jumble_count
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time


  jumble_reference:
    type: object
    description: resource definitions for jumble_reference
    properties:
      mem_mb:
        type: integer
        description: max memory in MB to be available
      mem_per_cpu:
        type: integer
        description: memory in MB used per cpu
      partition:
        type: string
        description: partition to use on cluster
      threads:
        type: integer
        description: number of threads to be available
      time:
        type: string
        description: max execution time

required:
  - default_resources
